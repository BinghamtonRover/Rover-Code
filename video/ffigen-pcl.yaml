# Run with `dart run ffigen --config ffigen.yaml -v severe`.
name: PointCloudBindings
description: |
  Bindings for PointCloud obstacle detection.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml -v severe`.
output: "lib/src/generated/pointcloud_ffi_bindings.dart"
headers:
  entry-points:
    - "src/pointcloud_ffi.h"
  include-directives:
    - "src/pointcloud_ffi.h"
comments:
  style: any
  length: full
